//1
let sum = 0;
const arr = [];
    for( let i = 0; arr.length < 7; i += 5) {
        arr.push(i);
        sum += i
    };
    sum / arr.length;

    let sum = 0;
const arr = [];
    for( let i = 0; arr.length < 7; i ++ 5) {
        arr.push(i*5);
    
    };

//2.1
const newArray = arr.map(item => item.length)

//2.2
arr.sort(); // [9, 9, 10, 12, A, K, R, S]

//2.3  ???

